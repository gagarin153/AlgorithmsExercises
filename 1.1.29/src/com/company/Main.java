package com.company;

public class Main {

    //Equal key. Add to BinarySearch a static method rank() that takes a sorted array of int values (some which may be equal) and a key as arguments
    //and returns the number of elements that are smaller than the key and a similar method count() that returns the number of elements
    // equal to the key. Note: If i and j are values returned by rank(a, key) and count(a, key) respectively, then a[i..i+j-1] are the values in
    // the array that equal to key.
    public static void main(String[] args) {
	// write your code here
    }
}
